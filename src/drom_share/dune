; generated by drom from package skeleton 'library'

(library
  (name drom_share)
  (public_name drom_share)
  (wrapped true)
  (libraries  )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package drom_share))

(copy_files ../../share/drom)
(rule
  (targets files.ml)
  (deps
   (:data
     (source_tree drom)))
  (action
      (run %{bin:ocaml-crunch} -m plain -o files.ml ../../share/drom/))
)

