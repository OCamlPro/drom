; generated by drom from package skeleton 'library'

(library
  (name drom_toml)
  (public_name drom_toml)
  (wrapped true)
  (libraries menhirLib ISO8601 str unix)
  
  
  )

(menhir (modules menhir_parser))
(ocamllex lexer)

(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package drom_toml))


