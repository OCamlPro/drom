; generated by drom from package skeleton 'library'

(library
  (name drom_lib)
  (public_name drom_lib)
  (wrapped true)
  (libraries toml opam-file-format ez_subst ez_file ez_config ez_cmdliner directories bigarray)
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package drom_lib))

